#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int test_case;
    scanf("%d", &test_case);

    for(int i=1; i<=test_case; i++)
    {
        int n, x, y;
        scanf("%d %d %d", &n, &x, &y);
        int answer = 0;
        printf("%d\n", answer);
    }
}